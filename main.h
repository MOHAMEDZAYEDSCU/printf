#ifndef MAIN_H
#define MAIN_H

#include <stdio.h>
#include <stdarg.h>
#include <limits.h>
#include <string.h>

int _putchar(char c);
void _printf(char *format, ...);
void _vprintf(char *format, va_list args);

#endif
